<?xml version="1.0" ?>
<!--
Licensed to the Apache Software Foundation (ASF) under one or more
contributor license agreements.  See the NOTICE file distributed with
this work for additional information regarding copyright ownership.
The ASF licenses this file to you under the Apache License, Version 2.0
(the "License"); you may not use this file except in compliance with
the License.  You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
-->
<Root>
  <TestCase name="testDiffPartitionKeysWithDiffOrderKeys1">
    <Resource name="sql">
      <![CDATA[
SELECT
    SUM(a) OVER (PARTITION BY b ORDER BY a),
    MAX(a) OVER (PARTITION BY b ORDER BY c),
    AVG(a) OVER (PARTITION BY c ORDER BY a),
    RANK() OVER (PARTITION BY b ORDER BY a),
    MIN(a) OVER (PARTITION BY c ORDER BY a)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), null:INTEGER) AS EXPR$0, MAX($0) OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST) AS EXPR$1, CAST(/(CASE(>(COUNT($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST), null:INTEGER), COUNT($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST))):INTEGER AS EXPR$2, RANK() OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$3, MIN($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST) AS EXPR$4])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[CASE(>($3, 0), $4, null:INTEGER) AS EXPR$0, w1$o0 AS EXPR$1, CAST(/(CASE(>($7, 0), $8, null:INTEGER), $7)):INTEGER AS EXPR$2, w0$o2 AS EXPR$3, w2$o2 AS EXPR$4])
+- LogicalWindow(window#0=[window(partition {1} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), RANK()])], window#1=[window(partition {1} order by [2 ASC-nulls-first] aggs [MAX($0)])], window#2=[window(partition {2} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), MIN($0)])])
   +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testDiffPartitionKeysWithDiffOrderKeys2">
    <Resource name="sql">
      <![CDATA[
SELECT
    SUM(a) OVER (PARTITION BY b ORDER BY c),
    MAX(a) OVER (PARTITION BY c ORDER BY a),
    MIN(a) OVER (ORDER BY c, a),
    RANK() OVER (PARTITION BY b ORDER BY c),
    AVG(a) OVER (ORDER BY b)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST), null:INTEGER) AS EXPR$0, MAX($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST) AS EXPR$1, MIN($0) OVER (ORDER BY $2 NULLS FIRST, $0 NULLS FIRST) AS EXPR$2, RANK() OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST) AS EXPR$3, CAST(/(CASE(>(COUNT($0) OVER (ORDER BY $1 NULLS FIRST), 0), $SUM0($0) OVER (ORDER BY $1 NULLS FIRST), null:INTEGER), COUNT($0) OVER (ORDER BY $1 NULLS FIRST))):INTEGER AS EXPR$4])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[CASE(>($6, 0), $7, null:INTEGER) AS EXPR$0, w1$o0 AS EXPR$1, w2$o0 AS EXPR$2, w0$o2 AS EXPR$3, CAST(/(CASE(>($3, 0), $4, null:INTEGER), $3)):INTEGER AS EXPR$4])
+- LogicalWindow(window#0=[window(order by [1 ASC-nulls-first] aggs [COUNT($0), $SUM0($0)])], window#1=[window(order by [2 ASC-nulls-first, 0 ASC-nulls-first] aggs [MIN($0)])], window#2=[window(partition {1} order by [2 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), RANK()])], window#3=[window(partition {2} order by [0 ASC-nulls-first] aggs [MAX($0)])])
   +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testDiffPartitionKeysWithSameOrderKeys">
    <Resource name="sql">
      <![CDATA[
SELECT
    SUM(a) OVER (PARTITION BY b ORDER BY a),
    MAX(a) OVER (PARTITION BY b ORDER BY a),
    AVG(a) OVER (PARTITION BY c ORDER BY a),
    RANK() OVER (PARTITION BY b ORDER BY a),
    MIN(a) OVER (PARTITION BY c ORDER BY a)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), null:INTEGER) AS EXPR$0, MAX($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$1, CAST(/(CASE(>(COUNT($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST), null:INTEGER), COUNT($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST))):INTEGER AS EXPR$2, RANK() OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$3, MIN($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST) AS EXPR$4])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[CASE(>($3, 0), $4, null:INTEGER) AS EXPR$0, w0$o2 AS EXPR$1, CAST(/(CASE(>($7, 0), $8, null:INTEGER), $7)):INTEGER AS EXPR$2, w0$o3 AS EXPR$3, w1$o2 AS EXPR$4])
+- LogicalWindow(window#0=[window(partition {1} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), MAX($0), RANK()])], window#1=[window(partition {2} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), MIN($0)])])
   +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testMultiOverWindowRangeType">
    <Resource name="sql">
      <![CDATA[
SELECT
    COUNT(*) OVER (PARTITION BY c ORDER BY a RANGE BETWEEN -1 PRECEDING AND 10 FOLLOWING),
    SUM(a) OVER (PARTITION BY c ORDER BY a),
    RANK() OVER (PARTITION BY c ORDER BY a, c),
    SUM(a) OVER (PARTITION BY c ORDER BY a RANGE BETWEEN 1 PRECEDING AND 10 FOLLOWING),
    COUNT(*) OVER (PARTITION BY c ORDER BY c ROWS BETWEEN 1 PRECEDING AND 10 FOLLOWING)
 FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[COUNT() OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST RANGE BETWEEN -1 PRECEDING AND 10 FOLLOWING) AS EXPR$0, CASE(>(COUNT($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST), null:INTEGER) AS EXPR$1, RANK() OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST, $2 NULLS FIRST) AS EXPR$2, CASE(>(COUNT($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST RANGE BETWEEN 1 PRECEDING AND 10 FOLLOWING), 0), $SUM0($0) OVER (PARTITION BY $2 ORDER BY $0 NULLS FIRST RANGE BETWEEN 1 PRECEDING AND 10 FOLLOWING), null:INTEGER) AS EXPR$3, COUNT() OVER (PARTITION BY $2 ORDER BY $2 NULLS FIRST ROWS BETWEEN 1 PRECEDING AND 10 FOLLOWING) AS EXPR$4])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[w0$o0 AS EXPR$0, CASE(>($3, 0), $4, null:INTEGER) AS EXPR$1, w2$o0 AS EXPR$2, CASE(>($5, 0), $6, null:INTEGER) AS EXPR$3, w4$o0 AS EXPR$4])
+- LogicalWindow(window#0=[window(partition {1} order by [0 ASC-nulls-first] range between $2 PRECEDING and $3 FOLLOWING aggs [COUNT()])], window#1=[window(partition {1} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0)])], window#2=[window(partition {1} order by [0 ASC-nulls-first] range between $4 PRECEDING and $3 FOLLOWING aggs [COUNT($0), $SUM0($0)])], window#3=[window(partition {1} order by [0 ASC-nulls-first, 1 ASC-nulls-first] aggs [RANK()])], window#4=[window(partition {1} order by [1 ASC-nulls-first] rows between $4 PRECEDING and $3 FOLLOWING aggs [COUNT()])])
   +- LogicalProject(select=[a, c])
      +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testSamePartitionKeysWithDiffOrderKeys1">
    <Resource name="sql">
      <![CDATA[
SELECT
    SUM(a) OVER (PARTITION BY b ORDER BY c),
    MAX(a) OVER (PARTITION BY b ORDER BY b),
    AVG(a) OVER (PARTITION BY b ORDER BY a),
    RANK() OVER (PARTITION BY b ORDER BY c),
    MIN(a) OVER (PARTITION BY b ORDER BY b)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST), null:INTEGER) AS EXPR$0, MAX($0) OVER (PARTITION BY $1 ORDER BY $1 NULLS FIRST) AS EXPR$1, CAST(/(CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), null:INTEGER), COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST))):INTEGER AS EXPR$2, RANK() OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST) AS EXPR$3, MIN($0) OVER (PARTITION BY $1 ORDER BY $1 NULLS FIRST) AS EXPR$4])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[CASE(>($7, 0), $8, null:INTEGER) AS EXPR$0, w1$o0 AS EXPR$1, CAST(/(CASE(>($3, 0), $4, null:INTEGER), $3)):INTEGER AS EXPR$2, w0$o2 AS EXPR$3, w1$o1 AS EXPR$4])
+- LogicalWindow(window#0=[window(partition {1} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0)])], window#1=[window(partition {1} order by [1 ASC-nulls-first] aggs [MAX($0), MIN($0)])], window#2=[window(partition {1} order by [2 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), RANK()])])
   +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testSamePartitionKeysWithDiffOrderKeys2">
    <Resource name="sql">
      <![CDATA[
SELECT
    SUM(a) OVER (PARTITION BY b ORDER BY c),
    MAX(a) OVER (PARTITION BY b ORDER BY a),
    AVG(a) OVER (PARTITION BY b ORDER BY a, c),
    RANK() OVER (PARTITION BY b ORDER BY a, b),
    MIN(a) OVER (PARTITION BY b ORDER BY b)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST), null:INTEGER) AS EXPR$0, MAX($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$1, CAST(/(CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST, $2 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST, $2 NULLS FIRST), null:INTEGER), COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST, $2 NULLS FIRST))):INTEGER AS EXPR$2, RANK() OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST, $1 NULLS FIRST) AS EXPR$3, MIN($0) OVER (PARTITION BY $1 ORDER BY $1 NULLS FIRST) AS EXPR$4])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[CASE(>($8, 0), $9, null:INTEGER) AS EXPR$0, w1$o0 AS EXPR$1, CAST(/(CASE(>($5, 0), $6, null:INTEGER), $5)):INTEGER AS EXPR$2, w3$o0 AS EXPR$3, w4$o0 AS EXPR$4])
+- LogicalWindow(window#0=[window(partition {1} order by [0 ASC-nulls-first] aggs [MAX($0)])], window#1=[window(partition {1} order by [0 ASC-nulls-first, 1 ASC-nulls-first] aggs [RANK()])], window#2=[window(partition {1} order by [0 ASC-nulls-first, 2 ASC-nulls-first] aggs [COUNT($0), $SUM0($0)])], window#3=[window(partition {1} order by [1 ASC-nulls-first] aggs [MIN($0)])], window#4=[window(partition {1} order by [2 ASC-nulls-first] aggs [COUNT($0), $SUM0($0)])])
   +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testSamePartitionKeysWithSameOrderKeysDiffDirection1">
    <Resource name="sql">
      <![CDATA[
SELECT
    SUM(a) OVER (PARTITION BY b ORDER BY a ASC),
    MAX(a) OVER (PARTITION BY b ORDER BY a ASC),
    AVG(a) OVER (PARTITION BY b ORDER BY a DESC),
    RANK() OVER (PARTITION BY b ORDER BY a ASC),
    MIN(a) OVER (PARTITION BY b ORDER BY a DESC)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), null:INTEGER) AS EXPR$0, MAX($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$1, CAST(/(CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 DESC NULLS LAST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $0 DESC NULLS LAST), null:INTEGER), COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 DESC NULLS LAST))):INTEGER AS EXPR$2, RANK() OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$3, MIN($0) OVER (PARTITION BY $1 ORDER BY $0 DESC NULLS LAST) AS EXPR$4])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[CASE(>($2, 0), $3, null:INTEGER) AS EXPR$0, w0$o2 AS EXPR$1, CAST(/(CASE(>($6, 0), $7, null:INTEGER), $6)):INTEGER AS EXPR$2, w0$o3 AS EXPR$3, w1$o2 AS EXPR$4])
+- LogicalWindow(window#0=[window(partition {1} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), MAX($0), RANK()])], window#1=[window(partition {1} order by [0 DESC-nulls-last] aggs [COUNT($0), $SUM0($0), MIN($0)])])
   +- LogicalProject(select=[a, b])
      +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testSamePartitionKeysWithSameOrderKeysDiffDirection2">
    <Resource name="sql">
      <![CDATA[
SELECT
    RANK() OVER (PARTITION BY b ORDER BY a DESC),
    RANK() OVER (PARTITION BY b ORDER BY a ASC)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[RANK() OVER (PARTITION BY $1 ORDER BY $0 DESC NULLS LAST) AS EXPR$0, RANK() OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$1])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[w0$o0 AS $0, w1$o0 AS $1])
+- LogicalWindow(window#0=[window(partition {1} order by [0 ASC-nulls-first] aggs [RANK()])], window#1=[window(partition {1} order by [0 DESC-nulls-last] aggs [RANK()])])
   +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testSamePartitionKeysWithSameOrderKeysPrefix">
    <Resource name="sql">
      <![CDATA[
SELECT a,
    RANK() OVER (PARTITION BY b ORDER BY c, a DESC),
    RANK() OVER (PARTITION BY b ORDER BY c, b)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[a, RANK() OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST, $0 DESC NULLS LAST) AS EXPR$1, RANK() OVER (PARTITION BY $1 ORDER BY $2 NULLS FIRST, $1 NULLS FIRST) AS EXPR$2])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[a, w0$o0 AS $1, w1$o0 AS $2])
+- LogicalWindow(window#0=[window(partition {1} order by [2 ASC-nulls-first, 0 DESC-nulls-last] aggs [RANK()])], window#1=[window(partition {1} order by [2 ASC-nulls-first, 1 ASC-nulls-first] aggs [RANK()])])
   +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
  <TestCase name="testSamePartitionKeysWithSameOrderKeysWithEmptyOrder">
    <Resource name="sql">
      <![CDATA[
SELECT
    SUM(a) OVER (PARTITION BY b ORDER BY a),
    MIN(a) OVER (PARTITION BY b),
    MAX(a) OVER (PARTITION BY b ORDER BY a)
FROM MyTable
      ]]>
    </Resource>
    <Resource name="ast">
      <![CDATA[
LogicalProject(select=[CASE(>(COUNT($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), 0), $SUM0($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST), null:INTEGER) AS EXPR$0, MIN($0) OVER (PARTITION BY $1) AS EXPR$1, MAX($0) OVER (PARTITION BY $1 ORDER BY $0 NULLS FIRST) AS EXPR$2])
+- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
    <Resource name="optimized rel plan">
      <![CDATA[
LogicalProject(select=[CASE(>($3, 0), $4, null:INTEGER) AS EXPR$0, w1$o0 AS EXPR$1, w0$o2 AS EXPR$2])
+- LogicalWindow(window#0=[window(partition {1} aggs [MIN($0)])], window#1=[window(partition {1} order by [0 ASC-nulls-first] aggs [COUNT($0), $SUM0($0), MAX($0)])])
   +- LogicalProject(select=[a, b])
      +- LogicalTableScan(table=[[default_catalog, default_database, MyTable]])
]]>
    </Resource>
  </TestCase>
</Root>
