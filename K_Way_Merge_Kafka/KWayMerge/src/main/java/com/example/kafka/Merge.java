package com.example.kafka;
import java.util.ArrayList;
public class Merge {
    private int[] merge(ArrayList[] arr) {
        int K = arr.length;
        int N = arr[0].size();


        return new int[0];
    }

}
