/* eslint-disable @typescript-eslint/no-explicit-any */

/**
 * @deprecated We should avoid using type any. Instead, try to infer the type or use unknown as an alternative.
 */
export type SafeAny = any;
